/** Called once per window, listens for global events & synchronizes store's state across all windows
  * Gives an option to append custom callback for any event that's being listened for by this hook */
const useListenGlobal = () => {
  return {}
}

export default useListenGlobal;
